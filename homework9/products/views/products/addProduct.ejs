<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="stylesheet" href="/stylesheets/style.css" />
		<title>Document</title>
	</head>
	<body>
		<div>
			<% if (errors?.length > 0) { %>
			<ul>
				<% errors.forEach(function(error) { %>
				<li><%= error.msg %></li>
				<% }) %>
			</ul>
			<hr />
			<% } %>
		</div>
		<hr />
		<form action="/products/add" method="post">
			<input type="text" placeholder="enter title" required value="<%= data?.title %>" name="title" />
			<input type="number" min="1" placeholder="enter price" value="<%= data?.price %>" required name="price" />
			<input type="number" min="1" max="100" required placeholder="enter amount" value="<%= data?.amount %>" name="amount" />
			<button type="submit">add</button>
		</form>
	</body>
</html>
